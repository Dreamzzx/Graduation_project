# 视频推流系统流程文档

## 一、整体流程概览

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                    数据流向                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
│  │   摄像头      │    │  VideoSource │    │ capture_queue│    │   Detector   │       │
│  │  (MJPG输出)   │───▶│   采集模块    │───▶│   帧队列     │───▶│   检测模块    │       │
│  └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘       │
│         │                   │                                        │              │
│         │              CUDA解码                                      │              │
│         │                   │                                        │              │
│         ▼                   ▼                                        ▼              │
│  ┌──────────────┐    ┌──────────────┐                         ┌──────────────┐      │
│  │  dshow采集    │    │  cv::Mat     │                         │  FrameData   │      │
│  │  MJPG压缩流   │    │  BGR24格式    │                         │  + 检测结果   │      │
│  └──────────────┘    └──────────────┘                         └──────────────┘      │
│                                                                      │              │
│                                                                      ▼              │
│                                                               ┌──────────────┐     │
│                                                               │ detect_queue │     │
│                                                               │   检测队列    │     │
│                                                               └──────────────┘     │
│                                                                      │              │
│                         ┌────────────────────────────────────────────┤              │
│                         │                                            │              │
│                         ▼                                            ▼              │
│                  ┌──────────────┐                            ┌──────────────┐       │
│                  │ VideoRecorder│                            │  FFmpegPush  │       │
│                  │   录制模块    │                            │   推流模块    │       │
│                  └──────────────┘                            └──────────────┘       │
│                         │                                            │              │
│                         ▼                                            ▼              │
│                  ┌──────────────┐                            ┌──────────────┐       │
│                  │  MP4文件      │                            │  RTSP服务器   │       │
│                  │  本地存储     │                            │   网络推流    │       │
│                  └──────────────┘                            └──────────────┘       │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、阶段一：视频采集（VideoSource）

### 2.1 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                      VideoSource 采集流程                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────┐    ┌────────────┐    ┌────────────┐             │
│  │ 初始化设备  │───▶│ 配置参数   │───▶│ 打开设备   │             │
│  └────────────┘    └────────────┘    └────────────┘             │
│        │                                    │                   │
│        ▼                                    ▼                   │
│  ┌────────────┐                      ┌────────────┐             │
│  │avdevice_   │                      │avformat_   │             │
│  │register_all│                      │open_input  │             │
│  └────────────┘                      └────────────┘             │
│                                             │                   │
│                                             ▼                   │
│                                      ┌────────────┐             │
│                                      │ 初始化解码器 │             │
│                                      └────────────┘             │
│                                             │                   │
│                                             ▼                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      采集循环                            │   │
│  │  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌───────┐ │   │
│  │  │av_read_  │──▶│avcodec_  │──▶│avcodec_  │──▶│格式   │ │   │
│  │  │frame     │   │send_packet│   │receive_  │   │转换   │ │   │
│  │  │(读取帧)   │   │(发送包)   │   │frame     │   │BGR24  │ │   │
│  │  └──────────┘   └──────────┘   └──────────┘   └───────┘ │   │
│  │        │              │              │            │      │   │
│  │        ▼              ▼              ▼            ▼      │   │
│  │   MJPG压缩包    解码器输入      NV12帧      cv::Mat     │   │
│  │                                              BGR24      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│                       ┌────────────┐                            │
│                       │ capture_   │                            │
│                       │ queue.Push │                            │
│                       └────────────┘                            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `device_name` | `video=HP Wide Vision HD Camera` | DirectShow 设备名称 |
| `input_format` | `dshow` | 输入格式（Windows DirectShow） |
| `vcodec` | `mjpeg` | 摄像头输出编码格式 |
| `video_size` | `640x360` | 采集分辨率 |
| `framerate` | `24` | 采集帧率 |
| `rtbufsize` | `2000000` | 环形缓冲区大小（字节） |

### 2.3 关键 API

#### 初始化阶段

| API | 功能 | 参数说明 |
|-----|------|----------|
| `avdevice_register_all()` | 注册所有设备 | 无参数 |
| `av_find_input_format("dshow")` | 查找 dshow 输入格式 | `"dshow"` - Windows DirectShow |
| `avformat_alloc_context()` | 分配格式上下文 | 返回 `AVFormatContext*` |
| `av_dict_set()` | 设置采集参数 | `key, value, flags` |
| `avformat_open_input()` | 打开输入设备 | `&format_ctx, device_name, input_format, &options` |
| `avformat_find_stream_info()` | 获取流信息 | `format_ctx, nullptr` |
| `av_find_best_stream()` | 查找视频流 | `format_ctx, AVMEDIA_TYPE_VIDEO` |

#### CUDA 硬件解码初始化

| API | 功能 | 参数说明 |
|-----|------|----------|
| `av_hwdevice_ctx_create()` | 创建 CUDA 设备上下文 | `&hw_device_ctx, AV_HWDEVICE_TYPE_CUDA, nullptr, nullptr, 0` |
| `avcodec_find_decoder_by_name("mjpeg_cuvid")` | 查找 CUDA MJPEG 解码器 | `"mjpeg_cuvid"` - NVIDIA 硬件解码器 |
| `avcodec_alloc_context3()` | 分配解码器上下文 | `codec` |
| `avcodec_parameters_to_context()` | 复制流参数到上下文 | `codec_ctx, video_stream->codecpar` |
| `av_buffer_ref()` | 引用硬件设备上下文 | `hw_device_ctx` |
| `av_hwframe_ctx_alloc()` | 分配硬件帧上下文 | `hw_device_ctx` |
| `av_hwframe_ctx_init()` | 初始化硬件帧上下文 | `hw_frames_ref` |
| `avcodec_open2()` | 打开解码器 | `codec_ctx, codec, &options` |

#### 采集循环

| API | 功能 | 参数说明 |
|-----|------|----------|
| `av_packet_alloc()` | 分配数据包 | 返回 `AVPacket*` |
| `av_read_frame()` | 读取一帧数据 | `format_ctx, packet` |
| `avcodec_send_packet()` | 发送包到解码器 | `codec_ctx, packet` |
| `avcodec_receive_frame()` | 接收解码后的帧 | `codec_ctx, frame` |
| `av_hwframe_transfer_data()` | GPU 帧传输到 CPU | `sw_frame, hw_frame, 0` |
| `sws_getContext()` | 创建格式转换上下文 | `src_w, src_h, src_fmt, dst_w, dst_h, dst_fmt, flags` |
| `sws_scale()` | 执行格式转换 | `sws_ctx, src_data, src_linesize, ...` |
| `av_frame_free()` | 释放帧 | `&frame` |
| `av_packet_unref()` | 取消引用包 | `packet` |
| `av_packet_free()` | 释放包 | `&packet` |

### 2.4 数据格式转换

```
摄像头输出: MJPG (压缩格式)
     │
     ▼ av_read_frame()
AVPacket (MJPG 压缩数据)
     │
     ▼ avcodec_send_packet() + avcodec_receive_frame()
AVFrame (NV12 格式, GPU 显存)
     │
     ▼ av_hwframe_transfer_data()
AVFrame (NV12 格式, CPU 内存)
     │
     ▼ sws_scale()
cv::Mat (BGR24 格式)
     │
     ▼ frame_queue->Push()
capture_queue (线程安全队列)
```

---

## 三、阶段二：目标检测（YOLOv11Detector）

### 3.1 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                      YOLOv11 检测流程                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────┐    ┌────────────┐    ┌────────────┐             │
│  │ capture_   │───▶│  图像预处理 │───▶│  模型推理   │             │
│  │ queue.Pop  │    │  Letterbox  │    │  ONNX GPU  │             │
│  └────────────┘    └────────────┘    └────────────┘             │
│        │                 │                 │                    │
│        ▼                 ▼                 ▼                    │
│   cv::Mat          640x640 RGB        Tensor输出               │
│   BGR24                                  │                      │
│                                          ▼                      │
│                                   ┌────────────┐                │
│                                   │  后处理     │                │
│                                   │  NMS过滤   │                │
│                                   └────────────┘                │
│                                          │                      │
│                                          ▼                      │
│                                   ┌────────────┐                │
│                                   │  绘制框     │                │
│                                   │  标注结果   │                │
│                                   └────────────┘                │
│                                          │                      │
│                                          ▼                      │
│                                   ┌────────────┐                │
│                                   │ detect_    │                │
│                                   │ queue.Push │                │
│                                   └────────────┘                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `model_path` | `model/yolo11n.onnx` | ONNX 模型路径 |
| `input_width` | `640` | 模型输入宽度 |
| `input_height` | `640` | 模型输入高度 |
| `confidence_threshold` | `0.5` | 置信度阈值 |
| `nms_threshold` | `0.45` | NMS 阈值 |
| `classes` | `["person"]` | 检测类别 |

### 3.3 关键 API（ONNX Runtime）

| API | 功能 | 参数说明 |
|-----|------|----------|
| `Ort::Env()` | 创建 ONNX 环境 | `ORT_LOGGING_LEVEL_WARNING, "YOLOv11"` |
| `Ort::SessionOptions()` | 创建会话选项 | 设置 GPU 提供者 |
| `OrtCUDAProviderOptions()` | CUDA 提供者选项 | `device_id=0` |
| `SessionOptions.AppendExecutionProvider_CUDA()` | 添加 CUDA 支持 | `cuda_options` |
| `Ort::Session()` | 创建推理会话 | `env, model_path, session_options` |
| `session.GetInputName()` | 获取输入节点名 | `index, allocator` |
| `session.GetOutputName()` | 获取输出节点名 | `index, allocator` |
| `session.Run()` | 执行推理 | `run_options, input_names, inputs, output_names` |

### 3.4 数据结构

```cpp
struct FrameData {
    cv::Mat frame;           // 检测后的图像（带标注）
    bool has_person;         // 是否检测到人物
    std::vector<BoxInfo> boxes;  // 检测框列表
    int64_t timestamp_ms;    // 时间戳
};
```

---

## 四、阶段三：RTSP 推流（FFmpegPush）

### 4.1 流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FFmpegPush 推流流程                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  初始化阶段                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 网络初始化 │─▶│ CUDA初始化│─▶│ 格式上下文 │─▶│ 视频流创建 │─▶│ 编码器配置 │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
│       │              │              │              │              │         │
│       ▼              ▼              ▼              ▼              ▼         │
│  avformat_      av_hwdevice_   avformat_alloc_  avformat_new_   h264_nvenc │
│  network_init   ctx_create     output_context2  stream                      │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                         推流循环                                      │   │
│  │                                                                       │   │
│  │   ┌────────────┐    ┌────────────┐    ┌────────────┐    ┌─────────┐ │   │
│  │   │ detect_    │───▶│  BGR转NV12 │───▶│ 上传到GPU  │───▶│ 设置PTS │ │   │
│  │   │ queue.Pop  │    │  sws_scale │    │hwframe_    │    │         │ │   │
│  │   └────────────┘    └────────────┘    │transfer_data│   └─────────┘ │   │
│  │         │                 │                 │               │        │   │
│  │         ▼                 ▼                 ▼               ▼        │   │
│  │    FrameData          sw_frame          hw_frame         pts递增    │   │
│  │    cv::Mat            NV12 CPU          NV12 GPU                    │   │
│  │                                                                       │   │
│  │   ┌────────────┐    ┌────────────┐    ┌────────────┐    ┌─────────┐ │   │
│  │   │ 发送帧编码 │───▶│ 接收编码包 │───▶│ 时间戳转换 │───▶│ 发送网络│ │   │
│  │   │avcodec_    │    │avcodec_    │    │av_packet_  │    │av_write_│ │   │
│  │   │send_frame  │    │receive_pkt │    │rescale_ts  │    │frame    │ │   │
│  │   └────────────┘    └────────────┘    └────────────┘    └─────────┘ │   │
│  │         │                 │                 │               │        │   │
│  │         ▼                 ▼                 ▼               ▼        │   │
│  │     GPU编码          AVPacket          90kHz PTS        RTSP服务器  │   │
│  │     NVENC            H.264 NAL                                       │   │
│  │                                                                       │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 关键参数

#### 编码器参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `codec` | `h264_nvenc` | NVIDIA 硬件编码器 |
| `width` | `1920` | 输出视频宽度 |
| `height` | `1080` | 输出视频高度 |
| `bit_rate` | `5000000` | 码率 (5Mbps) |
| `time_base` | `{1, 90000}` | MPEG 90kHz 时间基 |
| `framerate` | `{24, 1}` | 帧率 |
| `gop_size` | `15` | GOP 大小（约 0.625 秒） |
| `max_b_frames` | `0` | 无 B 帧（低延迟） |
| `pix_fmt` | `AV_PIX_FMT_CUDA` | GPU 像素格式 |

#### NVENC 私有参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `preset` | `p4` | 编码预设（性能优先） |
| `rc` | `cbr` | 码率控制模式（恒定码率） |
| `delay` | `0` | 编码延迟（零延迟） |
| `repeat_header` | `1` | 重复 SPS/PPS 头 |
| `forced-idr` | `1` | 强制 IDR 帧 |

#### RTSP 连接参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `rtsp_transport` | `tcp` | RTSP 传输协议 |
| `stimeout` | `5000000` | 超时时间（微秒） |
| `max_delay` | `500000` | 最大延迟（微秒） |

### 4.3 关键 API

#### 初始化阶段

| API | 功能 | 参数说明 |
|-----|------|----------|
| `avformat_network_init()` | 初始化网络 | 无参数 |
| `av_hwdevice_ctx_create()` | 创建 CUDA 设备上下文 | `&hw_device_ctx, AV_HWDEVICE_TYPE_CUDA, "0"` |
| `avformat_alloc_output_context2()` | 创建输出上下文 | `&fmt_ctx, nullptr, "rtsp", url` |
| `avcodec_find_encoder_by_name()` | 查找编码器 | `"h264_nvenc"` |
| `avformat_new_stream()` | 创建视频流 | `fmt_ctx, nullptr` |
| `avcodec_alloc_context3()` | 分配编码器上下文 | `codec` |
| `av_opt_set()` | 设置编码器私有参数 | `codec_ctx->priv_data, key, value, 0` |
| `av_hwframe_ctx_alloc()` | 分配硬件帧上下文 | `hw_device_ctx` |
| `av_hwframe_ctx_init()` | 初始化硬件帧上下文 | `hw_frames_ref` |
| `avcodec_open2()` | 打开编码器 | `codec_ctx, codec, &options` |
| `avcodec_parameters_from_context()` | 复制参数到流 | `video_stream->codecpar, codec_ctx` |
| `avformat_write_header()` | 写 RTSP 头 | `fmt_ctx, &opts` |

#### 推流循环

| API | 功能 | 参数说明 |
|-----|------|----------|
| `av_frame_alloc()` | 分配帧 | 返回 `AVFrame*` |
| `av_packet_alloc()` | 分配包 | 返回 `AVPacket*` |
| `av_frame_get_buffer()` | 为帧分配缓冲区 | `sw_frame, alignment` |
| `av_hwframe_get_buffer()` | 获取 GPU 帧缓冲区 | `hw_frames_ctx, hw_frame, 0` |
| `av_frame_make_writable()` | 使帧可写 | `sw_frame` |
| `sws_scale()` | BGR 转 NV12 | `sws_ctx, src_data, src_linesize, ...` |
| `av_hwframe_transfer_data()` | 上传帧到 GPU | `hw_frame, sw_frame, 0` |
| `avcodec_send_frame()` | 发送帧到编码器 | `codec_ctx, hw_frame` |
| `avcodec_receive_packet()` | 接收编码后的包 | `codec_ctx, pkt` |
| `av_packet_rescale_ts()` | 时间戳转换 | `pkt, src_time_base, dst_time_base` |
| `av_write_frame()` | 写入网络流 | `fmt_ctx, pkt` |
| `av_packet_unref()` | 取消引用包 | `pkt` |

### 4.4 PTS 时间戳处理

```cpp
// 时间基: {1, 90000} (MPEG 90kHz 标准)
const AVRational src_time_base = {1, 90000};

// 帧间隔计算
frame_duration = 90000 / fps;  // 24fps → 3750 ticks/帧

// PTS 递增
current_pts += frame_duration;

// 帧时间戳设置
hw_frame->pts = current_pts;
hw_frame->pkt_dts = current_pts;

// 包时间戳转换（源和目标时间基相同，无需缩放）
av_packet_rescale_ts(pkt, {1, 90000}, video_stream->time_base);
```

### 4.5 数据格式转换

```
detect_queue.Pop()
     │
     ▼
FrameData.frame (cv::Mat, BGR24, 1920x1080)
     │
     ▼ sws_scale()
sw_frame (AVFrame, NV12, CPU 内存)
     │
     ▼ av_hwframe_transfer_data()
hw_frame (AVFrame, NV12, GPU 显存)
     │
     ▼ avcodec_send_frame() + avcodec_receive_packet()
AVPacket (H.264 NAL 单元)
     │
     ▼ av_write_frame()
RTSP 网络流
```

---

## 五、阶段四：视频录制（VideoRecorder）

### 5.1 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    VideoRecorder 录制流程                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  触发条件: frame_data.has_person == true                         │
│                                                                  │
│  ┌────────────┐    ┌────────────┐    ┌────────────┐             │
│  │ 检测到人物  │───▶│ 开始录制   │───▶│ 写入帧     │             │
│  │ onPerson   │    │startRecord │    │ writeFrame │             │
│  │ Detected   │    │ ing        │    │            │             │
│  └────────────┘    └────────────┘    └────────────┘             │
│                                              │                   │
│                                              ▼                   │
│                                       ┌────────────┐             │
│                                       │ BGR转YUV420│             │
│                                       │ sws_scale  │             │
│                                       └────────────┘             │
│                                              │                   │
│                                              ▼                   │
│                                       ┌────────────┐             │
│                                       │ 编码写入   │             │
│                                       │ av_interleaved│          │
│                                       │ _write_frame│            │
│                                       └────────────┘             │
│                                                                  │
│  停止条件: 人物离开超过 timeout (默认 3000ms)                      │
│                                                                  │
│  ┌────────────┐    ┌────────────┐    ┌────────────┐             │
│  │ 人物离开   │───▶│ 超时检测   │───▶│ 停止录制   │             │
│  │ onPerson   │    │ elapsed >  │    │ stopRecord │             │
│  │ Left       │    │ timeout    │    │ ing        │             │
│  └────────────┘    └────────────┘    └────────────┘             │
│                                              │                   │
│                                              ▼                   │
│                                       ┌────────────┐             │
│                                       │ 写入文件尾 │             │
│                                       │ av_write_  │             │
│                                       │ trailer    │             │
│                                       └────────────┘             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `width` | `1920` | 输出视频宽度 |
| `height` | `1080` | 输出视频高度 |
| `fps` | `24` | 输出帧率 |
| `bitrate` | `5000000` | 编码码率 |
| `time_base` | `{1, 90000}` | MPEG 90kHz 时间基 |
| `pix_fmt` | `AV_PIX_FMT_YUV420P` | 像素格式 |
| `gop_size` | `fps * 2` | GOP 大小 |
| `max_b_frames` | `0` | 无 B 帧 |
| `output_format` | `mp4` | 输出格式 |
| `person_leave_timeout_ms` | `3000` | 人物离开超时时间 |

### 5.3 关键 API

| API | 功能 | 参数说明 |
|-----|------|----------|
| `avformat_alloc_output_context2()` | 创建输出上下文 | `&fmt_ctx, nullptr, "mp4", filename` |
| `avcodec_find_encoder()` | 查找编码器 | `AV_CODEC_ID_H264` |
| `avcodec_alloc_context3()` | 分配编码器上下文 | `codec` |
| `avcodec_open2()` | 打开编码器 | `codec_ctx, codec, nullptr` |
| `avformat_new_stream()` | 创建视频流 | `fmt_ctx, nullptr` |
| `avio_open()` | 打开输出文件 | `&fmt_ctx->pb, filename, AVIO_FLAG_WRITE` |
| `avformat_write_header()` | 写文件头 | `fmt_ctx, nullptr` |
| `sws_getContext()` | 创建格式转换上下文 | `BGR24 → YUV420P` |
| `sws_scale()` | 执行格式转换 | `sws_ctx, src_data, ...` |
| `avcodec_send_frame()` | 发送帧到编码器 | `codec_ctx, yuv_frame` |
| `avcodec_receive_packet()` | 接收编码后的包 | `codec_ctx, pkt` |
| `av_interleaved_write_frame()` | 写入帧 | `fmt_ctx, pkt` |
| `av_write_trailer()` | 写文件尾 | `fmt_ctx` |
| `avio_closep()` | 关闭文件 | `&fmt_ctx->pb` |

### 5.4 PTS 时间戳处理

```cpp
// 基于实际时间计算 PTS
auto now = std::chrono::steady_clock::now();
auto now_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
    now.time_since_epoch()).count();

if (start_time_ms == 0) {
    start_time_ms = now_ms;  // 记录开始时间
}

int64_t elapsed_ms = now_ms - start_time_ms;

// 毫秒转 90kHz
yuv_frame->pts = elapsed_ms * 90;
```

---

## 六、线程模型

```
┌─────────────────────────────────────────────────────────────────┐
│                         线程池架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    ThreadPool                            │    │
│  │                                                          │    │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │    │
│  │   │   Thread 1  │  │   Thread 2  │  │   Thread 3  │     │    │
│  │   │             │  │             │  │             │     │    │
│  │   │ VideoSource │  │  Detector   │  │   Recorder  │     │    │
│  │   │   采集线程   │  │   检测线程   │  │   录制线程   │     │    │
│  │   └─────────────┘  └─────────────┘  └─────────────┘     │    │
│  │         │                 │                 │            │    │
│  │         ▼                 ▼                 ▼            │    │
│  │   capture_queue    detect_queue      MP4 文件           │    │
│  │                                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   主线程 (Main Thread)                   │    │
│  │                                                          │    │
│  │   FFmpegPush::start() → 推流初始化                       │    │
│  │   FFmpegPush::pushFrame() → 推流循环（阻塞式）            │    │
│  │                                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 七、队列数据流

```
┌──────────────────────────────────────────────────────────────────────────┐
│                              队列数据流                                   │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  VideoSource                    YOLOv11Detector                 FFmpegPush │
│       │                              │                               │    │
│       ▼                              ▼                               ▼    │
│  ┌─────────────┐              ┌─────────────┐              ┌─────────────┐│
│  │capture_queue│              │detect_queue │              │   推流输出   ││
│  │             │              │             │              │             ││
│  │ VideoFrame  │───Pop()───▶  │ FrameData   │───Pop()───▶  │ RTSP Stream ││
│  │ Queue<cv::  │              │ Queue<Frame │              │             ││
│  │ Mat>        │              │ Data>       │              │             ││
│  │             │              │             │              │             ││
│  │ 容量: 2     │              │ 容量: 3     │              │             ││
│  └─────────────┘              └─────────────┘              └─────────────┘│
│       │                              │                               │    │
│       │         数据结构              │                               │    │
│       ▼                              ▼                               ▼    │
│  ┌─────────────┐              ┌─────────────┐                            │
│  │ cv::Mat     │              │ FrameData   │                            │
│  │             │              │             │                            │
│  │ - BGR24     │              │ - cv::Mat   │                            │
│  │ - 640x360   │              │ - has_person│                            │
│  │             │              │ - boxes     │                            │
│  │             │              │ - timestamp │                            │
│  └─────────────┘              └─────────────┘                            │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 八、完整数据流时间线

```
时间轴 ──────────────────────────────────────────────────────────────────────▶

T0: 程序启动
    │
    ├── ZPusher::Init()
    │   ├── 加载配置文件
    │   ├── 创建队列: capture_queue(2), detect_queue(3)
    │   ├── 初始化 VideoSource (摄像头 + CUDA解码)
    │   ├── 初始化 YOLOv11Detector (ONNX GPU)
    │   ├── 初始化 FFmpegPush (NVENC + RTSP)
    │   └── 初始化 VideoRecorder (MP4)
    │
T1: 开始推流
    │
    ├── ZPusher::start_Push()
    │   ├── is_pushing = true
    │   ├── Thread 1: video_source->start()     ──┐
    │   ├── Thread 2: yolov11_detector->start() ──┼── 并行执行
    │   ├── FFmpegPush::start() (主线程)        ──┤
    │   └── Thread 3: 推流循环                  ──┘
    │
T2: 采集循环 (Thread 1)
    │   │
    │   ├── av_read_frame() ──▶ MJPG Packet
    │   ├── avcodec_send_packet() + avcodec_receive_frame()
    │   │                       ──▶ NV12 Frame (GPU)
    │   ├── av_hwframe_transfer_data() ──▶ NV12 Frame (CPU)
    │   ├── sws_scale() ──▶ BGR24 cv::Mat
    │   └── capture_queue->Push(mat)
    │
T3: 检测循环 (Thread 2)
    │   │
    │   ├── capture_queue->Pop() ──▶ cv::Mat
    │   ├── Letterbox 预处理 ──▶ 640x640 RGB
    │   ├── ONNX 推理 ──▶ Tensor 输出
    │   ├── NMS 后处理 ──▶ 检测框
    │   ├── 绘制标注框
    │   └── detect_queue->Push(frame_data)
    │
T4: 推流循环 (主线程)
    │   │
    │   ├── detect_queue->Pop() ──▶ FrameData
    │   ├── 检查 has_person ──▶ 触发/停止录制
    │   ├── sws_scale() ──▶ NV12
    │   ├── av_hwframe_transfer_data() ──▶ GPU Frame
    │   ├── avcodec_send_frame() + avcodec_receive_packet()
    │   │                       ──▶ H.264 Packet
    │   ├── av_packet_rescale_ts() ──▶ 90kHz PTS
    │   └── av_write_frame() ──▶ RTSP 服务器
    │
T5: 录制触发 (Thread 3)
    │   │
    │   ├── has_person == true
    │   ├── VideoRecorder::onPersonDetected()
    │   ├── sws_scale() ──▶ YUV420P
    │   ├── avcodec_send_frame() + avcodec_receive_packet()
    │   └── av_interleaved_write_frame() ──▶ MP4 文件
    │
T6: 录制停止
    │   │
    │   ├── has_person == false && elapsed > timeout
    │   ├── VideoRecorder::onPersonLeft()
    │   ├── av_write_trailer()
    │   └── avio_closep()
    │
T7: 程序退出
    │
    └── ZPusher::Destroy()
        ├── is_pushing = false
        ├── video_recorder->stop()
        └── delete thread_pool
```

---

## 九、关键配置汇总

### config.json 完整配置

```json
{
    "push": {
        "rtsp_url": "rtsp://192.168.5.66:554/live/test",
        "video_width": 1920,
        "video_height": 1080,
        "frame_rate": 24,
        "video_bitrate": 5000000
    },
    "capture": {
        "device_name": "video=HP Wide Vision HD Camera",
        "input_format": "dshow",
        "video_width": 640,
        "video_height": 360,
        "framerate": 24
    },
    "detector": {
        "enabled": true,
        "model_path": "model/yolo11n.onnx",
        "confidence_threshold": 0.5,
        "input_width": 640,
        "input_height": 640,
        "classes": ["person"]
    },
    "recorder": {
        "enabled": true,
        "output_dir": "recordings",
        "person_leave_timeout_ms": 3000
    },
    "log": {
        "level": "info",
        "debug_enabled": false
    }
}
```

---

## 十、API 速查表

### 采集模块

| 阶段 | API | 说明 |
|------|-----|------|
| 初始化 | `avdevice_register_all()` | 注册设备 |
| | `av_find_input_format("dshow")` | 查找 dshow 格式 |
| | `avformat_open_input()` | 打开设备 |
| | `av_hwdevice_ctx_create()` | 创建 CUDA 上下文 |
| | `avcodec_find_decoder_by_name("mjpeg_cuvid")` | 查找解码器 |
| | `avcodec_open2()` | 打开解码器 |
| 采集 | `av_read_frame()` | 读取帧 |
| | `avcodec_send_packet()` | 发送到解码器 |
| | `avcodec_receive_frame()` | 接收解码帧 |
| | `av_hwframe_transfer_data()` | GPU→CPU |
| | `sws_scale()` | 格式转换 |

### 推流模块

| 阶段 | API | 说明 |
|------|-----|------|
| 初始化 | `avformat_network_init()` | 初始化网络 |
| | `avformat_alloc_output_context2()` | 创建输出上下文 |
| | `avcodec_find_encoder_by_name("h264_nvenc")` | 查找编码器 |
| | `av_hwframe_ctx_alloc()` | 分配 GPU 帧上下文 |
| | `avcodec_open2()` | 打开编码器 |
| | `avformat_write_header()` | 写 RTSP 头 |
| 推流 | `sws_scale()` | BGR→NV12 |
| | `av_hwframe_transfer_data()` | CPU→GPU |
| | `avcodec_send_frame()` | 发送到编码器 |
| | `avcodec_receive_packet()` | 接收编码包 |
| | `av_packet_rescale_ts()` | 时间戳转换 |
| | `av_write_frame()` | 写入网络流 |

### 录制模块

| 阶段 | API | 说明 |
|------|-----|------|
| 初始化 | `avformat_alloc_output_context2()` | 创建 MP4 上下文 |
| | `avio_open()` | 打开文件 |
| | `avformat_write_header()` | 写文件头 |
| 录制 | `sws_scale()` | BGR→YUV420P |
| | `avcodec_send_frame()` | 发送到编码器 |
| | `avcodec_receive_packet()` | 接收编码包 |
| | `av_interleaved_write_frame()` | 写入文件 |
| 结束 | `av_write_trailer()` | 写文件尾 |
| | `avio_closep()` | 关闭文件 |
